<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head') %>

  <body>
    <%- include('./partials/header') %>

    <div class="bg-zinc-900 h-screen w-full p-5">
      <div class="flex flex-col justify-center items-center">
        <h2 class="text-center text-white text-4xl mb-10 font-bold">Edit User</h2>

        <form
          class="text-zinc-400 flex flex-col w-1/3"
          enctype="multipart/form-data"
          id="editUserForm"
        >
          <input
            class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
            type="text"
            name="name"
            placeholder="Enter name"
            value="<%= user.name %>"
          />
          <input
            class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
            type="text"
            name="username"
            placeholder="Enter username"
            value="<%= user.username %>"
          />
          <input
            class="bg-transparent border border-zinc-600 p-3 rounded-lg mb-3"
            type="email"
            name="email"
            placeholder="Enter email"
            value="<%= user.email %>"
          />

          <div class="flex w-full items-center justify-between bg-grey-lighter mb-3">
            <label
              class="w-2/3 flex flex-col items-center px-4 py-8 bg-zinc-800 rounded-lg shadow-lg tracking-wide uppercase border border-zinc-500 cursor-pointer hover:bg-zinc-600 hover:text-white"
            >
              <svg
                class="w-8 h-8"
                fill="currentColor"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 20 20"
              >
                <path
                  d="M16.88 9.1A4 4 0 0 1 16 17H5a5 5 0 0 1-1-9.9V7a3 3 0 0 1 4.52-2.59A4.98 4.98 0 0 1 17 8c0 .38-.04.74-.12 1.1zM11 11h3l-4-4-4 4h3v3h2v-3z"
                />
              </svg>
              <span class="mt-2 text-base leading-normal">Select a profile image</span>
              <input type="file" name="image" class="hidden" />
            </label>
            <img class="w-1/3 ms-3" src="<%= user.image %>" alt="image" />
          </div>

          <input
            class="bg-blue-500 p-3 rounded-lg mb-3 text-white"
            type="submit"
            value="Edit User"
          />
        </form>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="../public/javascripts/editUser.js"></script>
  </body>
</html>
